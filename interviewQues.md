# I1 

* memory leakage - frontend / backend
* react.memo vs useMemo
* how would you call a callback function when a partiular section comes on view while scrolliing.
* is it okay to use math.random as keys of react lists. why?
* is indexing possible on candidate keys
* Object.freeze()
* Primitive and non primitive datatypes in javascript?
* filter array of object on basis of single / multiple keys
* frontend practices for 10k users vs 1 billion users
* security layers in an application - frontend / backend / Infra (cloud + DB) 
* bsics about test cases

# I2

* aggregation pipeline in mongo db
* design a database for library management system
* js code - settimeout with loop
* what is docker and why is it used - basic overview
* ci-cd pipeline stages and overview
* microservices vs monolith
* settimeout vs setimmediate
* primary key vs foreign key vs candidate keys
* how to apply middleware - use case - the middleware should run only once per session, how would you implement this?
* write a code to call api using fetch in js efficiently
* how to stream large json from backend and how to implement it in frontend


# I3

* do you have familiarity in redis, and datatypes used in redis.
* types of pagination - offset vs cursor - when to use each
* SQL injection and prevention - frontend and backend both
* caching strategies - frontend - (headers / redux / react-query/ localstorage )
* caching strategies - backend - (in memory / redis) - most common
* how will you implement global search in any website - hint - pg tsvector, elastic search
* practices to optimize fetch query - backend

# I4 - definitions only 

* how to implement authorization and authentications - backend
* microservices vs monolith - backend
* react lifecycle
* redux vs context api
* acid properties in sql
* callback hell and promises

# I5

* filter identical objects from array of object
* put vs patch http method
* different types of api - rest, soap, grpc, graphQL, websockets - what to use when
* practices to optimize fetch query - backend

# I6

* promise.all
* Hoisting
* CSR, SSR, SSG, ISR - what to use when 
* tanstack query vs RTK query
* useMemo vs useCallback
* react fiber and useTransition hook in react
* if we want to use react and angular in same project what complication will occur





